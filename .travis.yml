language: php

php:
  - 5.3
  - 5.5
  - 5.6
  - hhvm

before_script:
  - echo 'date.timezone = "Europe/Budapest"' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini
  - composer self-update
  - composer install --no-interaction

script: ./vendor/bin/phpunit
